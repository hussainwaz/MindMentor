# An OpenRouter key: https://openrouter.ai/keys
# The variable is named for the OpenAI SDK that reads it, but the client is
# pointed at OpenRouter's base URL.
OPENAI_API_KEY=sk-or-v1-...

# Optional overrides
DEFAULT_MODEL_NAME=DeepSeek
MAX_TOKENS=4000
TEMPERATURE=0.7
